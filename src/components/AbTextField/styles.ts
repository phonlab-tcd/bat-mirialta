import { TextFieldModel } from './types';

const styles: { [name: string]: TextFieldModel } = {
  bat: {
    label: 'scríobh anseo',
    rows: 1,
    disabled: false,
    autoFocus: true,
  },
};

export default styles;
